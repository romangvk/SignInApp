import React, { Component } from 'react';
import { Text, View } from 'react-native';
export default class FormsScreen extends Component {
    render() {
        return (
            <Text>Change forms.</Text>
        );
    }
}