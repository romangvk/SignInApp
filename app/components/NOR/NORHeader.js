import React, { Component } from 'react';
import { Text, View } from 'react-native';

export default class NORHeader extends Component {
    constructor(props) {
        super(props);
    }
    render() {
        return (<Text>This is the Header</Text>)
    }
}